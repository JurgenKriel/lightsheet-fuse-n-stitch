# Plan 02.5-01 Summary — Create mvstitch_env

**Phase:** 02.5-zarrstitcher-rework
**Plan:** 01
**Status:** Complete
**Date:** 2026-05-19
**Requirements satisfied:** STITCH-07

## What was built

A new conda env `mvstitch_env` at `/vast/scratch/users/kriel.j/mvstitch_env` containing the full multiview-stitcher stack pinned to the versions documented in `02.5-RESEARCH.md` §Standard Stack, plus a versioned `environment_mvstitch.yml` checked into `scripts/lightsheet_pipeline/` so other team members can recreate the env.

## Verified package pins (post-install)

| Package                  | Installed | Plan required        |
| ------------------------ | --------- | -------------------- |
| multiview-stitcher       | 0.1.52    | == 0.1.52            |
| spatial_image            | 1.2.3     | == 1.2.3             |
| multiscale_spatial_image | 2.0.3     | == 2.0.3             |
| ngff-zarr                | 0.34.0    | ≥ 0.12.2             |
| aicspylibczi             | 3.3.1     | == 3.3.1             |
| cupy-cuda12x             | 14.0.1    | == 14.0.1            |
| dask                     | 2025.10.0 | < 2025.11.0          |
| scipy / scikit-image / zarr | 1.17.1 / 0.26.0 / 3.1.6 | (latest) |

## STITCH-06 / lightsheet_env non-regression

`lightsheet_env`'s dask is still **2026.3.0** (verified). No accidental downgrade — the separate env achieves its purpose.

## Deviation log

**Task 2** — `conda env create -f environment_mvstitch.yml -p ...` failed because the system conda configuration has `defaults` as a channel, which is unreachable from this HPC login node (network egress blocked to the anaconda.com default mirror). The plan's Task 2 action explicitly authorizes a manual sequential pip-install fallback in this scenario; that fallback was used. End-state matches the plan exactly — same env path, same pinned versions.

## Commits

- `d43f94a` chore(02.5-01): add environment_mvstitch.yml — multiview-stitcher 0.1.52 conda env spec
- (this commit) docs(02.5-01): mark plan 01 complete + SUMMARY + STATE/ROADMAP/REQUIREMENTS update

## Files changed

- **Added:** `scripts/lightsheet_pipeline/environment_mvstitch.yml`
- **Added:** `scripts/lightsheet_pipeline/.planning/phases/02.5-zarrstitcher-rework/02.5-01-SUMMARY.md`
- **Modified:** `scripts/lightsheet_pipeline/.planning/STATE.md`, `ROADMAP.md`, `REQUIREMENTS.md`

## Downstream

Unblocks plan 02.5-02 (08_stitch.py Stage 1 register) — its Task 2 verification command `/vast/scratch/users/kriel.j/mvstitch_env/bin/python 08_stitch.py --stage register --help` now resolves.
