---
phase: 01-substack-notebook
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/lightsheet_pipeline/07_direct_fuse.sh
  - scripts/lightsheet_pipeline/07_direct_fuse_README.md
autonomous: true
requirements:
  - SUBSTACK-01

must_haves:
  truths:
    - "07_direct_fuse_README.md CLI flags table lists all 7 parameters including the 5 new ones"
    - "07_direct_fuse.sh contains a commented substack example showing --z-start / --z-end usage"
    - "07_direct_fuse.sh does not change any functional execution path — only documentation added"
  artifacts:
    - path: "scripts/lightsheet_pipeline/07_direct_fuse_README.md"
      provides: "Updated CLI reference table with 5 new flags"
      contains: "z-start"
    - path: "scripts/lightsheet_pipeline/07_direct_fuse.sh"
      provides: "SLURM wrapper with substack usage comment"
      contains: "z-start"
  key_links:
    - from: "07_direct_fuse_README.md CLI table"
      to: "07_direct_fuse.py parse_args()"
      via: "documentation mirroring actual CLI"
      pattern: "z-start"
---

<objective>
Update 07_direct_fuse_README.md to document the 5 new CLI flags added in Plan 01, and add a commented substack usage example to 07_direct_fuse.sh. No functional changes to the SLURM script.

Purpose: Ensures the docs are consistent with the modified script so the user knows how to call the substack workflow without reading source code.
Output: README with updated CLI table; SLURM script with substack comment block.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/vast/projects/BCRL_Multi_Omics/scripts/lightsheet_pipeline/.planning/PROJECT.md
@/vast/projects/BCRL_Multi_Omics/scripts/lightsheet_pipeline/07_direct_fuse_README.md
@/vast/projects/BCRL_Multi_Omics/scripts/lightsheet_pipeline/07_direct_fuse.sh
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update README CLI table and SLURM script comment</name>
  <files>
    scripts/lightsheet_pipeline/07_direct_fuse_README.md
    scripts/lightsheet_pipeline/07_direct_fuse.sh
  </files>
  <read_first>
    - scripts/lightsheet_pipeline/07_direct_fuse_README.md (full file — read before editing)
    - scripts/lightsheet_pipeline/07_direct_fuse.sh (full file — read before editing)
  </read_first>
  <action>
**Edit 07_direct_fuse_README.md — CLI flags table:**

Find the "CLI flags" table under `### CLI flags`. It currently has 7 rows (--czi, --out, --z-chunk, --workers, --skip-refine, --taper-px, --dump-layout). Replace the entire table with this expanded version:

```markdown
| Flag | Default | Purpose |
|---|---|---|
| `--czi` | KL018 staged path | Input CZI path. Use `/vast/scratch` copy for I/O throughput. |
| `--out` | `fused_direct.zarr` | Output OME-zarr path. |
| `--z-start` | 0 | First Z plane to process (inclusive). Substack: use 728. |
| `--z-end` | None (full stack) | Last Z plane to process (exclusive). Substack: use 828. |
| `--z-chunk` | 64 | Z planes processed per slab. Controls peak RAM (≈21 GB at 64). |
| `--workers` | 16 | Threads for parallel subblock reads. |
| `--skip-refine` | off | Skip NCC phase-correlation refinement; trust raw stage positions. |
| `--taper-px` | 64 | Cosine-taper width at tile edges for overlap blending. Increase to 128–144 to reduce seams. |
| `--sigma-frac` | 0.3 | Gaussian blend sigma fraction for dual-side fusion. Sweep 0.2–0.6. |
| `--fusion-axis` | 2 | Axis for dual-side blend: 0=Z, 1=Y, 2=X. KL018 uses X (2). |
| `--ncc-threshold` | 0.05 | NCC quality threshold for phase-correlation refinement. Raise to 0.1–0.2 to skip weak overlaps. |
| `--dump-layout` | off | Print tile bounding boxes and exit (no processing). |
```

Also add a new section after the CLI flags section and before "## Pipeline stages":

```markdown
## Substack workflow

To run fusion on a 100-slice substack (z=728:828, the central planes of KL018)
for rapid parameter tuning before committing to a full 1557-plane run:

```bash
# Interactive GPU node
conda activate /vast/scratch/users/kriel.j/lightsheet_env
module load CUDA/12.1
python 07_direct_fuse.py \
    --czi  /vast/scratch/users/kriel.j/KL018_lightsheet/KL018_85_D7_CT2AvIII_Overview.czi \
    --out  /vast/scratch/users/kriel.j/KL018_lightsheet/substack_z728_828.zarr \
    --z-start 728 --z-end 828 \
    --taper-px 128 \
    --sigma-frac 0.3 \
    --ncc-threshold 0.05 \
    --z-chunk 64 --workers 8
```

Expected runtime: ~16 min on A100 (2 channels × 2 Z-chunks × ~4 min each).
Output: `substack_z728_828.zarr` shape (1, 2, 100, H, W) + `ncc_scores.json`.
Use `08_fusion_dev.ipynb` (`Python (spatialdata_2025)` kernel) to inspect the
output and sweep parameters interactively.
```

**Edit 07_direct_fuse.sh — add a substack usage comment block:**

After the existing block comment at the top of the file (the lines starting with `# Direct CZI → fused.zarr...`), add the following comment block immediately before the `MANIFEST=` line:

```bash
# Substack example (100 planes for parameter tuning — ~16 min on A100):
#   python "${SCRIPT_DIR}/07_direct_fuse.py" \
#       --czi      "$CZI_ARG" \
#       --out      /vast/scratch/users/kriel.j/KL018_lightsheet/substack_z728_828.zarr \
#       --z-start  728 \
#       --z-end    828 \
#       --taper-px 128 \
#       --sigma-frac 0.3 \
#       --ncc-threshold 0.05 \
#       --z-chunk  64 \
#       --workers  8
#
# Full-stack flags (edit values here after substack validation):
#   --taper-px 64  --sigma-frac 0.3  --ncc-threshold 0.05  --fusion-axis 2
```

Do NOT change any executable lines in 07_direct_fuse.sh. Only add comments.
  </action>
  <verify>
    <automated>grep -c 'z-start' /vast/projects/BCRL_Multi_Omics/scripts/lightsheet_pipeline/07_direct_fuse_README.md</automated>
  </verify>
  <acceptance_criteria>
    - grep -c 'z-start' scripts/lightsheet_pipeline/07_direct_fuse_README.md returns >= 3 (table row, substack example, and substack section)
    - grep -c 'sigma-frac' scripts/lightsheet_pipeline/07_direct_fuse_README.md returns >= 2
    - grep -c 'ncc-threshold' scripts/lightsheet_pipeline/07_direct_fuse_README.md returns >= 2
    - grep -c 'fusion-axis' scripts/lightsheet_pipeline/07_direct_fuse_README.md returns >= 2
    - grep -c 'z-start' scripts/lightsheet_pipeline/07_direct_fuse.sh returns >= 1 (in comment)
    - grep -c '^#SBATCH' scripts/lightsheet_pipeline/07_direct_fuse.sh returns 7 (unchanged — no SBATCH lines added)
    - bash -n scripts/lightsheet_pipeline/07_direct_fuse.sh exits 0 (valid bash syntax)
  </acceptance_criteria>
  <done>README CLI table has 12 rows including all 5 new flags; README has new Substack workflow section; SLURM script has commented substack example; no functional lines in .sh were changed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Documentation only | No executable changes; no new trust boundaries introduced |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Tampering | 07_direct_fuse.sh | accept | Only comment lines added; bash -n validation confirms no executable change |
</threat_model>

<verification>
```bash
cd /vast/projects/BCRL_Multi_Omics/scripts/lightsheet_pipeline

# README contains new flags:
grep -E 'z-start|z-end|sigma-frac|fusion-axis|ncc-threshold' 07_direct_fuse_README.md

# SLURM script unchanged functionally:
bash -n 07_direct_fuse.sh && echo "PASS: valid bash syntax"
grep '^python' 07_direct_fuse.sh   # should still show just the single full-stack python call
```
</verification>

<success_criteria>
- README CLI table has rows for all 5 new flags
- README contains a "Substack workflow" section with the example command
- 07_direct_fuse.sh has substack example in comments but zero new executable lines
- bash -n 07_direct_fuse.sh exits 0
</success_criteria>

<output>
After completion, create `/vast/projects/BCRL_Multi_Omics/scripts/lightsheet_pipeline/.planning/phases/01-substack-notebook/01-02-SUMMARY.md`
</output>
